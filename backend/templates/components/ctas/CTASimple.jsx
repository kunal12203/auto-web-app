export default function CTA() {
  return (
    <section className="cta cta-simple">
      <div className="container">
        <h2>{{CTA_HEADLINE}}</h2>
        <p>{{CTA_SUBHEADLINE}}</p>
        <button className="cta-button">{{CTA_BUTTON_TEXT}}</button>
      </div>
    </section>
  )
}
